import React from 'react';
import style from './style.scss';

const Images = ({ images }) => {
  return (
    <div className={ style.images }>
    <img className={ style.test } src=""/>
    </div>  
  );
};

export default Images;